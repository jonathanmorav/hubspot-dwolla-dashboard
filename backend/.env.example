# Backend Configuration for OAuth Service
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=3001
NODE_ENV=development
# For production, set NODE_ENV=production

# Security - API Key for Extension Authentication
API_KEY=your-secure-api-key-here
# Generate a random string like: production-key-abc123xyz789

# Extension Security (Optional)
# Comma-separated list of allowed Chrome extension IDs for extra security
ALLOWED_EXTENSION_IDS=
# Example: ALLOWED_EXTENSION_IDS=abcdefghijk,lmnopqrstuv

# HubSpot OAuth Credentials
# Get these from: https://developers.hubspot.com/
HUBSPOT_CLIENT_ID=your_hubspot_client_id_here
HUBSPOT_CLIENT_SECRET=your_hubspot_client_secret_here

# Dwolla OAuth Credentials  
# Get these from: https://developers.dwolla.com/
DWOLLA_CLIENT_ID=your_dwolla_client_id_here
DWOLLA_CLIENT_SECRET=your_dwolla_client_secret_here
DWOLLA_ENVIRONMENT=sandbox
# Use 'sandbox' for testing, 'production' for live transactions

# DEPLOYMENT INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all "your_*_here" values with actual credentials
# 3. Never commit .env file to version control (it's in .gitignore)
# 4. For production deployment, set these same variables in your hosting platform