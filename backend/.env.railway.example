# Railway Environment Variables Template
# Copy these to your Railway dashboard

# Basic Configuration
PORT=3001
NODE_ENV=production

# Security
API_KEY=generate-a-secure-random-key-here
ALLOWED_EXTENSION_IDS=your-chrome-extension-id

# HubSpot OAuth Configuration
HUBSPOT_CLIENT_ID=your-hubspot-client-id
HUBSPOT_CLIENT_SECRET=your-hubspot-client-secret

# Dwolla Client Credentials Configuration
DWOLLA_CLIENT_ID=your-dwolla-client-id
DWOLLA_CLIENT_SECRET=your-dwolla-client-secret
DWOLLA_ENVIRONMENT=production  # Use 'sandbox' for testing

# Notes:
# 1. Generate a secure API_KEY using: openssl rand -base64 32
# 2. Get ALLOWED_EXTENSION_IDS from chrome://extensions after loading the extension
# 3. HubSpot credentials from: https://app.hubspot.com/developers/
# 4. Dwolla credentials from: https://accounts.dwolla.com/